import { Grid } from "@mui/material";
import React from "react";

const Blog = () => {
  return (
    <Grid
      sx={{
        display: "grid",
        placeItems: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <h1>Blog is Comming soon .....</h1>
    </Grid>
  );
};

export default Blog;
